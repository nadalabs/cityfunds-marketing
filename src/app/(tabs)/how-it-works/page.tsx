import PageTracker from '@components/common/PageTracker';
import _ from 'lodash';

export default async function IndexPage() {
  return (
    <PageTracker pageName="Index">
      <div></div>
    </PageTracker>
  );
}
